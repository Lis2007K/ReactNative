import React from "react";
import { Text, StyleSheet, View } from "react-native";

const ExerciseScreen = () => {
    let message = "Hi there"
    message = "I am learnign code"
    return(
        <Text>{message}</Text>
    )
}

export default ExerciseScreen;